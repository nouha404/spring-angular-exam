import { ResponsableServiceImpl } from './responsable.service.impl';

describe('ResponsableServiceImpl', () => {
  it('should create an instance', () => {
    expect(new ResponsableServiceImpl()).toBeTruthy();
  });
});
